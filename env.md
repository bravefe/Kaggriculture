# Kaggriculture Environment Report

Generated by running the repository virtual environment with Python 3.11.

## Command

```python
from kaggle_environments import make

env = make("kaggriculture", debug=True)
```

## Basic metadata

| Property | Value |
| --- | --- |
| Name | `kaggriculture` |
| Version | `0.1.0` |
| Agents | 2 |
| Episode steps | 720 |
| Turns per day | 24 |
| Season length | 30 days |
| Board size | 10 x 10 |
| Starting money | 3000 |
| Shed capacity | 100 items |
| Initial state entries | 2 |
| Initial status | `ACTIVE`, `ACTIVE` |

## Action specification

Each turn uses an object with three sections:

```python
{
    "farmer": ["PASS"],
    "hands": [],
    "market": []
}
```

### Farmer and hand operations

`NORTH`, `SOUTH`, `EAST`, `WEST`, `PASS`, `PICKUP`, `PLANT`, `WATER`,
`HARVEST`, `DROP`, `FERTILIZE`, `BUILD_COOP`, `BUILD_PASTURE`, `DIG`, `PLACE`,
`FEED`, `COLLECT_FERTILIZER`, and `CARE`.

### Market operations

`BUY_SEED`, `BUY_PRODUCT`, `BUY_ANIMAL`, `SELL`, `HIRE`, and `BUY_LAND`.

The action specification's default action is:

```python
{"farmer": ["PASS"], "hands": [], "market": []}
```

The environment permits up to 10 market orders per turn.

The local Gym wrapper also updates `env.specification.action.description` so
that `DROP` is included in the advertised farmer and hand operations.

## Runtime configuration

| Setting | Value |
| --- | ---: |
| `actTimeout` | 1 second |
| `runTimeout` | 1200 seconds |
| `boardSize` | 10 |
| `episodeSteps` | 720 |
| `turnsPerDay` | 24 |
| `startingMoney` | 3000 |
| `shedCapacity` | 100 |
| `maxMarketOrdersPerTurn` | 10 |
| `farmHandCostMult` | 1 |
| `townCenterSellInterval` | 24 turns |
| `townShopSellInterval` | 4 turns |
| `townShopUnlockInterval` | 3 turns |
| `weedSpawnChance` | 0.005 |
| `seed` | `None` |
| `marketParams` | `{}` |

## Initial state

At creation time:

- Current player: `0`
- Day: `0`
- Hour: `0`
- Both state entries are `ACTIVE`.
- There are two farms.
- Both farmers start at position `[4, 4]`.
- Both farms have 3,000 money.
- Both farms have no hired hands.
- Both farms have only the `NW` quadrant unlocked.
- The unlocked area is the upper-left 5x5 section of the 10x10 board.
- All other board cells are `LOCKED`.
- The initial town has no unlocked shops.

### Initial farm layout

Legend:

- `.` = empty, unlocked tile
- `X` = locked tile

```text
.....XXXXX
.....XXXXX
.....XXXXX
.....XXXXX
.....XXXXX
XXXXXXXXXX
XXXXXXXXXX
XXXXXXXXXX
XXXXXXXXXX
XXXXXXXXXX
```

### Initial market

All market inventory quantities start at `10000`:

| Product | Price |
| --- | ---: |
| `CARROT` | 35 |
| `EGG` | 50 |
| `FERTILIZER` | 100 |
| `MELON` | 250 |
| `MILK` | 160 |
| `STRAWBERRY` | 120 |
| `TOMATO` | 60 |
| `WHEAT` | 25 |
| `WOOL` | 200 |

### Initial private state

Each player starts with:

```python
inventories = [{}]
```

All seed counts are zero:

```python
{
    "CARROT": 0,
    "MELON": 0,
    "STRAWBERRY": 0,
    "TOMATO": 0,
    "WHEAT": 0
}
```

All shed counts are zero for:

```text
CARROT, COW, EGG, FERTILIZER, GOOSE, MELON,
MILK, SHEEP, STRAWBERRY, TOMATO, WHEAT, WOOL
```

## Notes

The environment prints OpenSpiel warnings about unavailable unrelated poker games when it is imported. These warnings do not prevent `kaggriculture` from being created successfully.
